#include <cstdio>
#include <bit>
#include <cstdint>
#include <string>
#include <chrono>
#include <iostream>
#include <bitset>

#if true
#define debug(pattern, ...) printf(pattern __VA_OPT__(, ) __VA_ARGS__)
#else
#define debug(...)
#endif


typedef unsigned char u8;
typedef uint16_t u16;
typedef uint64_t u64;


int main() {
    debug("Hello world %d", );
    return 0;
}